# Vanishing-points
A short python program which finds Vanishingpoints from images.
Edit find_siblings, make theta abs
rewrite things that are overly complicated ( like find_siblings, why is this not something that happens once, instead of so many times )
